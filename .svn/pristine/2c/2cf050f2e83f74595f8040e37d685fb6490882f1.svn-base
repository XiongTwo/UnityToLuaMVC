using UnityEngine;
using System.Collections;
using UnityEngine.SceneManagement;

public class MonoBehaviourTest2 : MonoBehaviour {

    // Use this for initialization
    void Awake()
    {
        Debug.Log("--------");


    }
    void Sta2rt()
    {
        Debug.Log(SceneManager.GetActiveScene().name + "--");
        var abc = SceneManager.GetSceneAt(1);


        SceneManager.SetActiveScene(abc);
        Debug.Log(SceneManager.GetActiveScene().name + "--" + abc.name);
        var abc1 = SceneManager.GetSceneAt(0);


        SceneManager.SetActiveScene(abc1);
        Debug.Log(SceneManager.GetActiveScene().name + "--" + abc1.name);
    }
        // Update is called once per frame
        void Update () {
	
	}
}
